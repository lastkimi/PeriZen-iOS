name: PostureGuardiOS
options:
  bundleIdPrefix: com.slmcamp
targets:
  PostureGuardiOS:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.slmcamp.PeriZen
      TARGETED_DEVICE_FAMILY: "1"
      INFOPLIST_FILE: PostureGuardiOS/Resources/Info.plist
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    sources:
      - path: PostureGuardiOS
